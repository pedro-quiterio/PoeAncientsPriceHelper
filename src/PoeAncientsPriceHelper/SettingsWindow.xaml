<Window x:Class="PoeAncientsPriceHelper.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="800" Width="398"
        MaxHeight="960"
        Icon="app.ico"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#999"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,6,0,2"/>
        </Style>

        <!-- Same flat button used on the main window: a TemplateBinding template so local
             Background/Foreground/BorderBrush always apply (WPF UI's implicit style ignores them). -->
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.35"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.82"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.68"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="Settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Start/Stop key:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,4">
            <TextBlock x:Name="HotkeyLabel" Text="F5" VerticalAlignment="Center"
                       FontFamily="Consolas" FontSize="12" MinWidth="46"/>
            <Button x:Name="RebindButton" Content="Rebind" Style="{StaticResource FlatButton}"
                    Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" Padding="10,2" Click="RebindButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Debug overlay key:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,4">
            <TextBlock x:Name="DebugHotkeyLabel" Text="F3" VerticalAlignment="Center"
                       FontFamily="Consolas" FontSize="12" MinWidth="46"/>
            <Button x:Name="RebindDebugButton" Content="Rebind" Style="{StaticResource FlatButton}"
                    Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" Padding="10,2" Click="RebindDebugButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Calibrate key:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,4">
            <TextBlock x:Name="CalibrateHotkeyLabel" Text="F4" VerticalAlignment="Center"
                       FontFamily="Consolas" FontSize="12" MinWidth="46"/>
            <Button x:Name="RebindCalibrateButton" Content="Rebind" Style="{StaticResource FlatButton}"
                    Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" Padding="10,2" Click="RebindCalibrateButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Theme:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="4" Grid.Column="1" x:Name="ThemeBox" Margin="0,4" MinWidth="120"
                  HorizontalAlignment="Left" SelectionChanged="ThemeBox_SelectionChanged"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Capture mode:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Vertical" Margin="0,4">
            <ComboBox x:Name="CaptureBox" MinWidth="160" HorizontalAlignment="Left"
                      SelectionChanged="CaptureBox_SelectionChanged"/>
            <TextBlock Text="(applies on next start)" Foreground="#666" FontSize="10" Margin="0,2,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Game language:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Vertical" Margin="0,4">
            <ComboBox x:Name="LanguageBox" MinWidth="160" HorizontalAlignment="Left"
                      SelectionChanged="LanguageBox_SelectionChanged"/>
            <TextBlock Text="Match your PoE 2 client language so item names are priced. (applies on next start)"
                       Foreground="#666" FontSize="10" TextWrapping="Wrap" Margin="0,2,0,0"/>
        </StackPanel>

        <CheckBox Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" x:Name="AutoStartBox"
                  Margin="0,12,0,0" Foreground="#CCC" FontSize="12"
                  Content="Auto-start scanning and minimize to tray on launch"
                  Checked="AutoStartBox_Changed" Unchecked="AutoStartBox_Changed"/>
        <TextBlock Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" VerticalAlignment="Top"
                   Text="Only applies once a region is calibrated. Just open the app and it starts in the tray."
                   Foreground="#666" FontSize="10" TextWrapping="Wrap" Margin="0,4,0,0"/>

        <StackPanel Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Vertical" Margin="0,14,0,0">
            <CheckBox x:Name="PauseWhenUnfocusedBox" Foreground="#CCC" FontSize="12"
                      Content="Only scan while Path of Exile is the active window"
                      Checked="PauseWhenUnfocusedBox_Changed" Unchecked="PauseWhenUnfocusedBox_Changed"/>
            <TextBlock Foreground="#666" FontSize="10" TextWrapping="Wrap" Margin="0,3,0,0"
                       Text="On by default: pricing pauses when you alt-tab away and resumes when you return. Turn off to keep scanning even when the game isn't in front (uses more CPU in the background)."/>
        </StackPanel>

        <StackPanel Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Vertical" Margin="0,14,0,0">
            <CheckBox x:Name="RumourEnabledBox" Foreground="#CCC" FontSize="12"
                      Content="Enable Island Rumour helper (experimental)"
                      Checked="RumourEnabledBox_Changed" Unchecked="RumourEnabledBox_Changed"/>
            <TextBlock Foreground="#888" FontSize="10" TextWrapping="Wrap" Margin="0,3,0,0"
                       Text="Experimental: auto-detects the Atlas rumour panel and shows ratings from a community spreadsheet. OCR can misread names and some rumours may be missing."/>
        </StackPanel>

        <TextBlock Grid.Row="11" Grid.Column="0" Text="Rumour scan rate:" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="11" Grid.Column="1" Orientation="Vertical" Margin="0,4">
            <ComboBox x:Name="RumourIntervalBox" MinWidth="160" HorizontalAlignment="Left"
                      SelectionChanged="RumourIntervalBox_SelectionChanged"/>
            <TextBlock Text="Only scans while the World map is open." Foreground="#666" FontSize="10" Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="12" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Vertical" Margin="0,12,0,0">
            <Button x:Name="RefreshRumoursButton" Content="Refresh rumour data from sheet"
                    Style="{StaticResource FlatButton}" Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" HorizontalAlignment="Left" Padding="10,4" Click="RefreshRumoursButton_Click"/>
            <Button x:Name="RumourSourceButton" Content="Open the data spreadsheet (community-maintained)"
                    Style="{StaticResource FlatButton}" Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" HorizontalAlignment="Left" Padding="10,4" Margin="0,6,0,0" Click="RumourSourceButton_Click"/>
            <TextBlock x:Name="RumourRefreshStatus" Text="" Foreground="#888" FontSize="10"
                       Margin="0,4,0,0" TextWrapping="Wrap"/>

            <Border Height="1" Background="#2A2A2A" Margin="0,12,0,10"/>
            <CheckBox x:Name="RumourAutoWorldBox" Foreground="#CCC" FontSize="12"
                      Content="Auto-detect the Atlas map (recommended)"
                      Checked="RumourAutoWorldBox_Changed" Unchecked="RumourAutoWorldBox_Changed"/>
            <TextBlock Foreground="#888" FontSize="10" TextWrapping="Wrap" Margin="0,3,0,0"
                       Text="The helper finds the &quot;WORLD&quot; label at the top-centre of the Atlas automatically. If rumours don't appear in windowed or custom-resolution modes, turn this off and mark where the WORLD label is yourself."/>
            <Button x:Name="SetWorldRegionButton" Content="Set WORLD region…"
                    Style="{StaticResource FlatButton}" Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444"
                    FontSize="11" HorizontalAlignment="Left" Padding="10,4" Margin="0,8,0,0" Click="SetWorldRegionButton_Click"/>
            <TextBlock x:Name="WorldRegionLabel" Text="" Foreground="#888" FontSize="10"
                       Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>
